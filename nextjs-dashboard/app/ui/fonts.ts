import { Inter } from 'next/font/google';
 import {lustitana} from 'next/font/google';

 export const lustitana = lustitana({   weight: ['400', '700'],
 subsets: ['latin'],
});
export const inter = Inter({ subsets: ['latin'] });